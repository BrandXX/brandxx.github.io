---
interface Props {
  title: string;
  subtitle?: string;
}

const { title, subtitle } = Astro.props;
---

<div>
  <h2 class="text-2xl font-semibold tracking-tight">{title}</h2>
  {subtitle && <p class="mt-2 max-w-3xl text-zinc-700 dark:text-zinc-300">{subtitle}</p>}
</div>
